import React from 'react';
import { db } from '../../firebase-config';

export default function SingleAd(target) {
  // db.collection('announcements')
  //   .where('id', '==', target.id)
  //   .onSnapshot(data => {
  //     console.log(data.author.value);
  //   });

  return (
    <>
      <div>UUuu</div>;<div>Uuuu</div>;
    </>
  );
}
